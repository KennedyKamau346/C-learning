# Ignore everything by default
*

# Do not ignore directories themselves, so we can see inside them
!*/

# Explicitly ONLY allow C files, headers, Makefiles, and git configuration
!*.c
!*.h
!Makefile
!.gitignore
!README.md
