/* Declares several int and float values without initializain and then prints them */
#include <stdio.h>

int main(void)
{
	int a, b, c, d;
	float e, f, g, h;

	printf("Integers: %d\t%d\t%d\t%d\n", a, b, c, d);
	printf("Floats: %f\t%f\t%f\t%f\n", e, f, g, h);

	return 0;
}
